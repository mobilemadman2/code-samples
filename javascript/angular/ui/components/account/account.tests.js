if (Meteor.isClient) {
  describe('controller', () => {
    // SCHOOL SUBSCRIPTIONS DETAILS
    it('Should display days left until membership runs out inside the circle', () => {
      expect(false).toBe(true);
    });

    it('Should display days left until membership runs out inside the circle', () => {
      expect(false).toBe(true);
    });

    // USERS
    it('Should open pop up form for creation of user when plus icon is clicked', () => {
      expect(false).toBe(true);
    });

    it('Disabled users cant log in', () => {
      expect(false).toBe(true);
    });

    it('After clicking "Disable" user goes to the "Inactive" tab', () => {
      expect(false).toBe(true);
    });

    it('After clicking "Enable" user goes to the "Active" tab', () => {
      expect(false).toBe(true);
    });

    it('Enabled users can log in', () => {
      expect(false).toBe(true);
    });

    // NOTIFICATION SETTINGS
    it('Should create notifications correspondingly to frequency selected ', () => {
      expect(false).toBe(true);
    });

    it('Should create notifications correspondingly to frequency selected ', () => {
      expect(false).toBe(true);
    });
  });
}
