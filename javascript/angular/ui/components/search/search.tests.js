if (Meteor.isClient) {
  describe('controller', () => {
    it('The top header should show number of all alumni registered', () => {
      expect(false).toBe(true);
    });

    it('Clicking "SELECT ALL" updates counter', () => {
      expect(false).toBe(true);
    });

    it('If no filters are selected, "CLEAR FILTERS" is not visible', () => {
      expect(false).toBe(true);
    });

    it('"CLEAR FILTERS" will reset any filters that have been set', () => {
      expect(false).toBe(true);
    });

    it('Selecting the column to be shown, activates the column', () => {
      expect(false).toBe(true);
    });

    it('Download button causes window to download the csv file', () => {
      expect(false).toBe(true);
    });

    it('Giving input activates a dropdown with suggestion', () => {
      expect(false).toBe(true);
    });

    it('After entering text alumni list decreases', () => {
      expect(false).toBe(true);
    });

    it('Hover over profile image causes select boxes to appear', () => {
      expect(false).toBe(true);
    });

    it('Clicking on the profile circle shows profile of that alumnus', () => {
      expect(false).toBe(true);
    });

    it('Selecting to show column "Job title" replaces columns', () => {
      expect(false).toBe(true);
    });

    it('Selecting to show column "Current status" replaces columns', () => {
      expect(false).toBe(true);
    });

    it('Show column "Undergraduate institution" replaces columns', () => {
      expect(false).toBe(true);
    });

    it('Selecting to show column "Current employer" replaces columns', () => {
      expect(false).toBe(true);
    });

    it('Selecting to show column "Mobile number" replaces columns', () => {
      expect(false).toBe(true);
    });

    // FILTERS - EDUCATION
    it('Selecting "Qualification type" will filter alumni list', () => {
      expect(false).toBe(true);
    });

    it('Selecting Post 16 > "Subject" will filter alumni list', () => {
      expect(false).toBe(true);
    });

    it('Selecting Undergraduate > "Institute" will filter alumni list', () => {
      expect(false).toBe(true);
    });

    it('Selecting Undergraduate > "Subject" will filter alumni list', () => {
      expect(false).toBe(true);
    });

    it('Selecting Postgraduate > "Institute" will filter alumni list', () => {
      expect(false).toBe(true);
    });

    it('Selecting Postgraduate > "Subject" will filter alumni list', () => {
      expect(false).toBe(true);
    });

    it('Selecting Apprenticeship > "Subject" will filter alumni list', () => {
      expect(false).toBe(true);
    });

    // FILTERS - EMPLOYMENT
    it('Selecting "Job title" will filter alumni list', () => {
      expect(false).toBe(true);
    });

    it('Selecting "Job sector" will filter alumni list', () => {
      expect(false).toBe(true);
    });

    it('Selecting "Employer" will filter alumni list', () => {
      expect(false).toBe(true);
    });

    // FILTERS - GENERAL
    it('Selecting "Gender" will filter alumni list', () => {
      expect(false).toBe(true);
    });

    it('Select "Able to visit in person" will filter alumni list', () => {
      expect(false).toBe(true);
    });

    it('Selecting "Occupation" will filter alumni list', () => {
      expect(false).toBe(true);
    });

    it('Selecting Leaving year > range will filter alumni list', () => {
      expect(false).toBe(true);
    });

    it('Selecting Support offered > "Option" will filter alumni list', () => {
      expect(false).toBe(true);
    });

    it('Selecting Sign up from > "Date" will filter alumni list', () => {
      expect(false).toBe(true);
    });

    it('Selecting Until > "Date" will filter alumni list', () => {
      expect(false).toBe(true);
    });

    // ACTIVITY HISTORY
    it('Selecting "Activity type" will filter alumni list', () => {
      expect(false).toBe(true);
    });

    it('Selecting Alumni activity > "Activity" will filter alumni list', () => {
      expect(false).toBe(true);
    });

    it('Selecting From > "Date" will filter alumni list', () => {
      expect(false).toBe(true);
    });

    it('Selecting Until > "Date" will filter alumni list', () => {
      expect(false).toBe(true);
    });

    it('Select "Has attended previous activity" > "Option" will filter', () => {
      expect(false).toBe(true);
    });

    // CUSTOM QUESTION - NOT DONE
    it('Select "Custom question" > "Option" will filter alumni', () => {
      expect(false).toBe(true);
    });

    // MULTIPLE FILTERS - NOT DONE
    it('Should apply filters correctly', () => {
      expect(false).toBe(true);
    });

    // SELECTING ALUMNI AND FAB BUTTON
    it('Selecting alumni activates activates FAB button', () => {
      expect(false).toBe(true);
    });

    it('Should open buttons Compose message, Email all alumni', () => {
      expect(false).toBe(true);
    });

    it('Should popup Invite to activity on click of Fab option', () => {
      expect(false).toBe(true);
    });

    it('Should open New email window on click of Fab option', () => {
      expect(false).toBe(true);
    });
  });
}
